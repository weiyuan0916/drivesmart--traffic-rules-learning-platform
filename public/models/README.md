Place optional 3D model files here:

- `mustang.glb` for the car simulation
- `motorcycle-large.glb` for the motorcycle figure-eight simulation

If files are missing, the app automatically falls back to built-in procedural models.
